# 🤖 Telegram-бот модератор

[![Python](https://img.shields.io/badge/python-3.10+-blue.svg)](https://www.python.org/)
[![Docker](https://img.shields.io/badge/docker-ready-blue.svg)](https://www.docker.com/)
[![MySQL](https://img.shields.io/badge/mysql-8.0+-orange.svg)](https://www.mysql.com/)

Локальный и модульный Telegram-бот для модерации чатов с использованием удалённой MySQL базы данных.

## 📋 Описание

Бот выполняет функции модератора в Telegram-чатах:
- Фильтрует нецензурные выражения
- Удаляет сообщения с запрещёнными словами
- Блокирует пользователей (команда /ban)
- Разблокирует пользователей (команда /unban)
- Логирует все действия
- Сохраняет статистику в удалённой MySQL базе данных (reg.ru)
- Работает в Docker-инфраструктуре

## 🏗️ Архитектура

Проект организован как **локальный** и **модульный**:
- **Локальный**: весь код и зависимости находятся в проекте
- **Модульный**: код разбит на независимые модули с чёткими границами

### Структура модулей

```
bot/
├── __init__.py      # Инициализация модуля
├── main.py          # Точка входа, координация модулей
├── config.py        # Конфигурация и переменные окружения
├── database.py      # Работа с MySQL через aiomysql
├── filters.py       # Фильтрация нецензурных слов
├── handlers.py      # Обработчики команд и сообщений
├── logger.py        # Настройка логирования
└── models.py        # Модели данных
```

## 🚀 Быстрый старт

### 1. Клонирование репозитория

```bash
# Клонируйте репозиторий
git clone https://github.com/YOUR_USERNAME/telegram-bot-moderator.git
cd telegram-bot-moderator

# Или скачайте архив и распакуйте
```

### 2. Настройка

```bash
# Скопируйте .env.example в .env
cp .env.example .env
# На Windows: copy .env.example .env

# Отредактируйте .env и заполните реальными данными:
# - BOT_TOKEN (получить у @BotFather)
# - Параметры подключения к MySQL на reg.ru
```

### 2. Настройка базы данных

1. Войдите в панель управления reg.ru
2. Перейдите в раздел "Базы данных" → "MySQL"
3. Сохраните параметры подключения в `.env`
4. Выполните SQL-скрипт `init.sql` через dbhub или phpMyAdmin

### 3. Запуск через Docker

```bash
# Сборка и запуск
docker-compose up --build

# Или в фоновом режиме
docker-compose up -d

# Просмотр логов
docker-compose logs -f moderator-bot

# Остановка
docker-compose down
```

### 4. Запуск локально (для разработки)

```bash
# Установка зависимостей
pip install -r requirements.txt

# Запуск бота
python -m bot.main
```

## 📝 Переменные окружения

Создайте файл `.env` в корне проекта:

```env
BOT_TOKEN=your_telegram_bot_token_here
DB_HOST=your_host.reg.ru
DB_PORT=3306
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_NAME=your_db_name
DATABASE_URL=mysql+aiomysql://user:password@host:3306/dbname
LOG_LEVEL=INFO
LOG_FILE=logs/bot.log
```

## 🤖 Команды бота

- `/start` - Начать работу с ботом
- `/help` - Показать справку
- `/stats` - Показать статистику модерации
- `/ban [user_id]` - Забанить пользователя (только для администраторов)
- `/unban [user_id]` - Разбанить пользователя (только для администраторов)

## 🔧 Настройка фильтров

Отредактируйте файл `bot/filters.py` и добавьте запрещённые слова в список `bad_words`:

```python
self.bad_words: Set[str] = {
    "слово1",
    "слово2",
    # Добавьте свои слова
}
```

## 📊 База данных

Таблица `bot_actions` хранит все действия бота:
- `id` - Уникальный идентификатор
- `action_type` - Тип действия (message_deleted, user_banned и т.д.)
- `user_id` - ID пользователя Telegram
- `username` - Имя пользователя
- `chat_id` - ID чата
- `message_text` - Текст сообщения
- `reason` - Причина действия
- `created_at` - Время создания записи

## 🐳 Docker

Проект полностью готов к работе в Docker:

```bash
# Сборка образа
docker-compose build

# Запуск
docker-compose up

# Остановка
docker-compose down
```

## 📦 Зависимости

- `aiogram==3.7.0` - Асинхронная библиотека для Telegram Bot API
- `aiomysql==0.2.0` - Асинхронный драйвер для MySQL
- `python-dotenv==1.0.0` - Загрузка переменных окружения
- `aiofiles==23.2.1` - Асинхронная работа с файлами

## 📝 Логирование

Логи сохраняются в:
- Файл: `logs/bot.log`
- Консоль: stdout

Уровни логирования: DEBUG, INFO, WARNING, ERROR

## 🔌 Работа с dbhub

dbhub уже установлен и подключен. Используйте его для:
- Выполнения SQL-запросов
- Просмотра данных в БД
- Мониторинга работы бота
- Создания таблиц (выполнение init.sql)

## 🛠️ Разработка

### Принципы модульности

- Каждый модуль имеет одну ответственность
- Модули взаимодействуют через чёткие интерфейсы
- Минимальные зависимости между модулями
- Каждый модуль можно тестировать независимо

### Добавление нового функционала

1. Создайте новый модуль в `bot/`
2. Импортируйте необходимые зависимости
3. Зарегистрируйте обработчики в `bot/handlers.py`
4. Обновите документацию

## 📦 Установка на VPS

Подробные инструкции по деплою на VPS для работы 24/7:
- [Полное руководство](DEPLOY_VPS.md)
- [Быстрый деплой](QUICK_DEPLOY.md)
- [Чеклист деплоя](VPS_CHECKLIST.md)

## 🤝 Вклад в проект

1. Fork проекта
2. Создайте ветку для новой функции (`git checkout -b feature/AmazingFeature`)
3. Закоммитьте изменения (`git commit -m 'Add some AmazingFeature'`)
4. Push в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📄 Лицензия

Проект создан в учебных целях.

## 👤 Автор

Telegram Bot Moderator Project

## 🙏 Благодарности

- [aiogram](https://github.com/aiogram/aiogram) - асинхронная библиотека для Telegram Bot API
- [aiomysql](https://github.com/aio-libs/aiomysql) - асинхронный драйвер для MySQL

## 🤝 Поддержка

При возникновении проблем:
1. Проверьте логи: `docker-compose logs moderator-bot`
2. Проверьте настройки в `.env`
3. Убедитесь, что бот добавлен в группу как администратор
4. Проверьте права бота в группе

---

**Версия:** 1.0.0  
**Автор:** Telegram Bot Moderator Project

