# РУКОВОДСТВО ПО РУЧНОМУ ТЕСТИРОВАНИЮ БОТА

## ✅ Автоматические тесты (выполнены)

- [x] Конфигурация - все параметры корректны
- [x] Фильтры нецензурных слов - работают правильно
- [x] Логирование - работает

## 📋 Ручное тестирование в Telegram

### 1. Тест базовых команд

**Команда `/start`**
- Отправьте в группе: `/start`
- Ожидаемый результат: Бот ответит приветственным сообщением

**Команда `/help`**
- Отправьте в группе: `/help`
- Ожидаемый результат: Бот покажет список доступных команд

**Команда `/stats`**
- Отправьте в группе: `/stats`
- Ожидаемый результат: Бот покажет статистику модерации

### 2. Тест фильтрации сообщений

**Тест 1: Обычное сообщение**
- Отправьте: "Привет, как дела?"
- Ожидаемый результат: Сообщение остаётся, не удаляется

**Тест 2: Оскорбление**
- Отправьте: "Ты дурак!"
- Ожидаемый результат: Сообщение удаляется ботом

**Тест 3: Другое оскорбление**
- Отправьте: "Идиот какой-то"
- Ожидаемый результат: Сообщение удаляется ботом

**Тест 4: Сложное оскорбление**
- Отправьте: "Это сволочь"
- Ожидаемый результат: Сообщение удаляется ботом

### 3. Тест сохранения в БД

После отправки оскорбления:
1. Проверьте логи: `docker-compose logs moderator-bot`
2. Должно быть: "Удалено сообщение от пользователя..."
3. Проверьте БД через dbhub:
   ```sql
   SELECT * FROM bot_actions ORDER BY created_at DESC LIMIT 5;
   ```
4. Должна появиться запись с `action_type = 'message_deleted'`

### 4. Тест команд модерации (только для администраторов)

**Команда `/ban`**
- Ответьте на сообщение пользователя командой `/ban`
- Или отправьте: `/ban [user_id]`
- Ожидаемый результат: Пользователь забанен, сообщение об этом

**Команда `/unban`**
- Отправьте: `/unban [user_id]`
- Ожидаемый результат: Пользователь разбанен

### 5. Проверка логов

```bash
# Просмотр логов в реальном времени
docker-compose logs -f moderator-bot

# Просмотр последних 50 строк
docker-compose logs --tail=50 moderator-bot

# Просмотр только ошибок
docker-compose logs moderator-bot | findstr ERROR
```

### 6. Проверка базы данных

**Через dbhub:**
```sql
-- Все действия
SELECT * FROM bot_actions ORDER BY created_at DESC LIMIT 10;

-- Статистика по типам
SELECT action_type, COUNT(*) as count 
FROM bot_actions 
GROUP BY action_type;

-- Действия за сегодня
SELECT * FROM bot_actions 
WHERE DATE(created_at) = CURDATE()
ORDER BY created_at DESC;
```

## ✅ Чеклист тестирования

- [ ] Команда /start работает
- [ ] Команда /help работает
- [ ] Команда /stats работает
- [ ] Обычные сообщения не удаляются
- [ ] Сообщения с оскорблениями удаляются
- [ ] Данные сохраняются в БД
- [ ] Логи записываются корректно
- [ ] Команда /ban работает (для админов)
- [ ] Команда /unban работает (для админов)

## 🐛 Если что-то не работает

1. Проверьте, что бот добавлен в группу
2. Проверьте, что бот - администратор
3. Проверьте права бота (удаление сообщений)
4. Проверьте логи: `docker-compose logs moderator-bot`
5. Проверьте подключение к БД в логах

