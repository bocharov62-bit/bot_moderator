# 🚀 Инструкция: Публикация на GitHub

## ✅ Что уже сделано:

1. ✅ Git репозиторий инициализирован
2. ✅ Все файлы добавлены в индекс
3. ✅ Первый коммит создан
4. ✅ .env защищён (не попадёт в репозиторий)

## 📋 Следующие шаги:

### Шаг 1: Создайте репозиторий на GitHub

1. Зайдите на [GitHub.com](https://github.com) и войдите
2. Нажмите **"+"** → **"New repository"**
3. Заполните:
   - **Repository name**: `telegram-bot-moderator` (или другое имя)
   - **Description**: `Telegram bot moderator with MySQL database and Docker support`
   - **Visibility**: Public или Private
   - **НЕ** создавайте README, .gitignore, license (у нас уже есть)
4. Нажмите **"Create repository"**

### Шаг 2: Подключите локальный репозиторий к GitHub

После создания репозитория GitHub покажет инструкции. Выполните:

```bash
# Замените YOUR_USERNAME и REPO_NAME на ваши данные
git remote add origin https://github.com/YOUR_USERNAME/REPO_NAME.git

# Проверка
git remote -v
```

### Шаг 3: Отправьте код на GitHub

```bash
# Переименовать ветку в main (если нужно)
git branch -M main

# Отправить код
git push -u origin main
```

Если GitHub попросит авторизацию:
- Используйте **Personal Access Token** (не пароль)
- Или настройте SSH ключи

### Шаг 4: Проверка

Откройте ваш репозиторий на GitHub и убедитесь:
- ✅ Все файлы загружены
- ✅ README.md отображается
- ✅ `.env` НЕ виден (не должен быть в репозитории!)
- ✅ `logs/` НЕ видна
- ✅ Код читаемый

## 🔒 Важно!

Убедитесь, что `.env` НЕ попал в репозиторий:
```bash
git check-ignore .env
# Должно вывести: .env
```

Если `.env` случайно попал в репозиторий:
1. Немедленно измените все секреты (BOT_TOKEN, пароли)
2. Удалите из истории: `git rm --cached .env`
3. Закоммитьте: `git commit -m "Remove .env"`
4. Отправьте: `git push`

## ✅ Готово!

После успешного push проект будет на GitHub! 🎉

