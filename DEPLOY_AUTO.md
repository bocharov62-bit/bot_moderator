# 🤖 ПОЛНОСТЬЮ АВТОМАТИЧЕСКИЙ ДЕПЛОЙ

## 🚀 Одна команда для деплоя

### Подключитесь к VPS и выполните:

```bash
ssh root@31.31.197.6
```

### Затем выполните эту команду (она сделает ВСЁ автоматически):

```bash
bash <(curl -s https://raw.githubusercontent.com/bocharov62-bit/bot_moderator/main/auto_deploy_complete.sh)
```

**Или загрузите скрипт:**

```bash
curl -o deploy.sh https://raw.githubusercontent.com/bocharov62-bit/bot_moderator/main/auto_deploy_complete.sh
bash deploy.sh
```

---

## ✅ Что делает скрипт автоматически:

1. ✅ Устанавливает Docker (если нет)
2. ✅ Устанавливает Docker Compose (если нет)
3. ✅ Клонирует репозиторий с GitHub
4. ✅ Создаёт .env файл
5. ✅ **Запрашивает данные для .env** (BOT_TOKEN, DB_USER, DB_PASSWORD и т.д.)
6. ✅ Создаёт директорию для логов
7. ✅ Собирает Docker образ
8. ✅ Запускает бота
9. ✅ Показывает статус и логи

---

## 📋 Что нужно будет ввести:

Скрипт попросит ввести:
- `BOT_TOKEN` - токен бота (8523401529:AAE7uNHCkJQGNZPOwnsnHl0RhgWiX3Ob1xY)
- `DB_HOST` - хост БД (31.31.197.6)
- `DB_PORT` - порт (3306)
- `DB_USER` - пользователь БД (u3359183_default)
- `DB_PASSWORD` - пароль БД
- `DB_NAME` - имя БД (u3359183_bot_ad_test)

**Всё остальное скрипт сделает сам!**

---

## 🎯 Альтернатива: полностью автоматический (с предзаполненными данными)

Если хотите, могу создать версию скрипта, которая автоматически использует ваши данные без запросов.

---

**Выполните команду на VPS - всё остальное сделается автоматически!** 🚀

