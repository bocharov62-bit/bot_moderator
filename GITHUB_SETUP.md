# 📦 Подготовка проекта к публикации на GitHub

## ✅ Чеклист перед публикацией

### 1. Проверка безопасности

- [x] `.env` в `.gitignore` - ✅ уже есть
- [x] `logs/` в `.gitignore` - ✅ уже есть
- [x] Резервные копии удалены - ✅ удалены
- [x] `.env.example` создан - ✅ создан
- [x] Чувствительные данные не в коде - ✅ проверено

### 2. Файлы для публикации

**Обязательные файлы:**
- ✅ `bot/` - весь код бота
- ✅ `Dockerfile` - образ Docker
- ✅ `docker-compose.yml` - конфигурация Docker
- ✅ `requirements.txt` - зависимости Python
- ✅ `init.sql` - SQL скрипт для БД
- ✅ `README.md` - документация
- ✅ `.env.example` - шаблон переменных окружения
- ✅ `.gitignore` - игнорируемые файлы
- ✅ `.gitattributes` - настройки Git

**Дополнительные файлы:**
- ✅ `DEPLOY_VPS.md` - инструкция по деплою
- ✅ `QUICK_DEPLOY.md` - быстрый деплой
- ✅ `VPS_CHECKLIST.md` - чеклист деплоя
- ✅ `deploy.sh` - скрипт деплоя
- ✅ `start.bat` / `start.sh` - скрипты запуска

**Исключены из репозитория:**
- ❌ `.env` - переменные окружения (секреты)
- ❌ `logs/` - логи работы
- ❌ `__pycache__/` - кэш Python
- ❌ `*.backup` - резервные копии
- ❌ `test_bot.py`, `check_env.py` - тестовые файлы

---

## 🚀 Шаги для публикации на GitHub

### Шаг 1: Инициализация Git репозитория

```bash
# В директории проекта
git init

# Проверка статуса
git status
```

### Шаг 2: Добавление файлов

```bash
# Добавить все файлы (кроме тех, что в .gitignore)
git add .

# Проверка что будет закоммичено
git status
```

**Важно:** Убедитесь, что `.env` НЕ добавлен в коммит!

### Шаг 3: Первый коммит

```bash
git commit -m "Initial commit: Telegram bot moderator

- Локальный и модульный проект
- Фильтрация нецензурных слов и оскорблений
- Интеграция с MySQL через aiomysql
- Docker поддержка
- Логирование и статистика"
```

### Шаг 4: Создание репозитория на GitHub

1. Зайдите на [GitHub.com](https://github.com)
2. Нажмите "New repository" (или "+" → "New repository")
3. Заполните:
   - **Repository name**: `telegram-bot-moderator` (или другое имя)
   - **Description**: "Telegram bot moderator with MySQL database and Docker support"
   - **Visibility**: Public или Private (на ваше усмотрение)
   - **НЕ** создавайте README, .gitignore или лицензию (у нас уже есть)
4. Нажмите "Create repository"

### Шаг 5: Подключение к GitHub

```bash
# Добавить remote (замените YOUR_USERNAME и REPO_NAME)
git remote add origin https://github.com/YOUR_USERNAME/REPO_NAME.git

# Или через SSH (если настроен)
git remote add origin git@github.com:YOUR_USERNAME/REPO_NAME.git

# Проверка
git remote -v
```

### Шаг 6: Отправка на GitHub

```bash
# Переименовать ветку в main (если нужно)
git branch -M main

# Отправить код
git push -u origin main
```

---

## 🔒 Безопасность

### Проверка перед push

```bash
# Проверить, что .env не попадёт в репозиторий
git check-ignore .env
# Должно вывести: .env

# Проверить список файлов для коммита
git ls-files | grep -E "\.env|logs/|__pycache__"
# Не должно быть вывода
```

### Если случайно добавили .env

```bash
# Удалить из индекса (но оставить локально)
git rm --cached .env

# Добавить в .gitignore (если ещё нет)
echo ".env" >> .gitignore

# Закоммитить изменения
git add .gitignore
git commit -m "Remove .env from repository"
```

---

## 📝 Рекомендации

### 1. Добавить описание репозитория

На странице репозитория GitHub:
- Добавьте описание проекта
- Укажите теги (tags): `telegram`, `bot`, `moderator`, `python`, `docker`, `mysql`
- Добавьте ссылку на документацию (если есть)

### 2. Создать файл LICENSE

Если хотите указать лицензию:
```bash
# Создать файл LICENSE (например, MIT)
# Или использовать готовый шаблон на GitHub
```

### 3. Настроить GitHub Actions (опционально)

Для автоматического тестирования и деплоя можно добавить `.github/workflows/`

### 4. Добавить badges в README

Пример:
```markdown
![Python](https://img.shields.io/badge/python-3.10+-blue.svg)
![Docker](https://img.shields.io/badge/docker-ready-blue.svg)
![License](https://img.shields.io/badge/license-MIT-green.svg)
```

---

## 🔄 Обновление репозитория

После изменений:

```bash
# Проверить изменения
git status

# Добавить изменения
git add .

# Закоммитить
git commit -m "Описание изменений"

# Отправить на GitHub
git push
```

---

## 📋 Финальная проверка

После публикации проверьте на GitHub:

- [ ] Все файлы загружены
- [ ] `.env` НЕ виден в репозитории
- [ ] `logs/` НЕ видна в репозитории
- [ ] README.md отображается корректно
- [ ] Код читаемый и структурированный
- [ ] `.env.example` присутствует

---

## 🎯 Готово!

Проект готов к публикации на GitHub! 🚀

